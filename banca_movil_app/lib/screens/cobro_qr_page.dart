
import 'package:flutter/material.dart';

class CobroQrPage extends StatelessWidget {
  const CobroQrPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('escaneando codigo qr'),
    );
  }
}